---
import Layout from '../../components/Layout.astro';
import { getCollection } from 'astro:content';

const allPoemas = await getCollection('poemas', ({ data }) => !data.draft);
allPoemas.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const allCuentos = await getCollection('cuentos', ({ data }) => !data.draft);
allCuentos.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const allPensamientos = await getCollection('pensamientos', ({ data }) => !data.draft);
allPensamientos.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<Layout activePage="words" title="Poética & Filosofía Zen" locale="es">

  <!-- Cabecera editorial de sala literaria -->
  <div style="margin-bottom: 44px; border-bottom: 1px solid var(--border-hairline); padding-bottom: 22px;">
    <span class="inv-code" style="margin-bottom: 8px;">ALCOBA LITERARIA &middot; EL VERBO Y EL SILENCIO</span>
    <h1 style="font-family: var(--font-display); font-size: clamp(2.1rem, 5.5vw, 3rem); font-weight: 400; color: var(--ink-primary); line-height: 1.12; margin-top: 4px; max-width: 22ch;">
      Poética &amp; Filosofía
    </h1>
    <p style="font-family: var(--font-display); font-size: 1.2rem; font-style: italic; color: var(--ink-muted); max-width: 56ch; margin-top: 10px; line-height: 1.7;">
      Versos mayores, relatos de la lentitud, el principio del Ayni andino y la estética Zen del vacío habitado.
    </p>
    <div style="margin-top: 20px; display: flex; gap: 16px; flex-wrap: wrap; font-family: var(--font-inscription); font-size: 0.7rem; letter-spacing: 0.14em; color: var(--ink-faint); text-transform: uppercase;">
      <a href="#poemas" style="color: var(--accent-gold); text-decoration: none; border-bottom: 1px solid var(--accent-gold); padding-bottom: 2px;">I — Poemas</a>
      <a href="#cuentos" style="color: inherit; text-decoration: none; border-bottom: 1px solid var(--border-hairline); padding-bottom: 2px;">II — Cuentos</a>
      <a href="#aforismos" style="color: inherit; text-decoration: none; border-bottom: 1px solid var(--border-hairline); padding-bottom: 2px;">III — Aforismos</a>
    </div>
  </div>

  <!-- I. POEMAS -->
  <section id="poemas" style="margin-bottom: 60px;">
    <div style="margin-bottom: 22px; border-bottom: 1px solid var(--border-hairline); padding-bottom: 10px; display: flex; justify-content: space-between; align-items: baseline;">
      <span class="inv-code">I &mdash; POEMAS &middot; VERBO SAGRADO</span>
      <span style="font-size: 0.78rem; color: var(--ink-faint);">{allPoemas.length} poemas</span>
    </div>
    <div style="display: grid; grid-template-columns: 1fr; gap: 20px;">
      {allPoemas.map((poema) => (
        <article class="zen-card" style="border-left: 2px solid var(--accent-gold);">
          <div style="display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 8px; flex-wrap: wrap; gap: 8px;">
            <span class="inv-code" style="margin-bottom: 0;">POEMA</span>
            <span style="font-size: 0.78rem; color: var(--ink-faint);">
              {poema.data.date.toLocaleDateString('es-ES', { year: 'numeric', month: 'short' })}
            </span>
          </div>
          <h2 class="zen-title">
            <a href={`/posts/poemas-${poema.id.replace(/\.md$/, '')}/`}>{poema.data.title}</a>
          </h2>
          {poema.data.dedication && (
            <p style="font-family: var(--font-display); font-size: 1.05rem; font-style: italic; color: var(--ink-muted); margin-bottom: 10px; line-height: 1.6;">
              &laquo;{poema.data.dedication}&raquo;
            </p>
          )}
          {poema.data.location && (
            <div style="font-size: 0.8rem; color: var(--ink-faint); margin-bottom: 14px;">
              &#128205; {poema.data.location}
            </div>
          )}
          <a href={`/posts/poemas-${poema.id.replace(/\.md$/, '')}/`} style="font-size: 0.85rem; font-weight: 600; color: var(--ink-primary); text-decoration: underline; text-underline-offset: 3px; min-height: 44px; display: inline-flex; align-items: center;">
            Leer poema &rarr;
          </a>
        </article>
      ))}
    </div>
  </section>

  <!-- II. CUENTOS -->
  {allCuentos.length > 0 && (
    <section id="cuentos" style="margin-bottom: 60px;">
      <div style="margin-bottom: 22px; border-bottom: 1px solid var(--border-hairline); padding-bottom: 10px; display: flex; justify-content: space-between; align-items: baseline;">
        <span class="inv-code">II &mdash; CUENTOS &middot; NARRATIVA DE LA LENTITUD</span>
        <span style="font-size: 0.78rem; color: var(--ink-faint);">{allCuentos.length} relatos</span>
      </div>
      <div style="display: grid; grid-template-columns: 1fr; gap: 20px;">
        {allCuentos.map((cuento) => (
          <article class="zen-card" style="border-left: 2px solid var(--sumi-400);">
            <div style="display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 8px; flex-wrap: wrap; gap: 8px;">
              <span class="inv-code" style="margin-bottom: 0;">CUENTO</span>
              <span style="font-size: 0.78rem; color: var(--ink-faint);">
                {cuento.data.date.toLocaleDateString('es-ES', { year: 'numeric', month: 'short' })}
              </span>
            </div>
            <h2 class="zen-title">
              <a href={`/posts/cuentos-${cuento.id.replace(/\.md$/, '')}/`}>{cuento.data.title}</a>
            </h2>
            <a href={`/posts/cuentos-${cuento.id.replace(/\.md$/, '')}/`} style="font-size: 0.85rem; font-weight: 600; color: var(--ink-primary); text-decoration: underline; text-underline-offset: 3px; min-height: 44px; display: inline-flex; align-items: center;">
              Leer relato &rarr;
            </a>
          </article>
        ))}
      </div>
    </section>
  )}

  <!-- III. AFORISMOS -->
  {allPensamientos.length > 0 && (
    <section id="aforismos" style="margin-bottom: 50px;">
      <div style="margin-bottom: 22px; border-bottom: 1px solid var(--border-hairline); padding-bottom: 10px; display: flex; justify-content: space-between; align-items: baseline;">
        <span class="inv-code">III &mdash; AFORISMOS &middot; FRAGMENTOS DE DIARIO</span>
        <span style="font-size: 0.78rem; color: var(--ink-faint);">{allPensamientos.length} fragmentos</span>
      </div>
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 18px;">
        {allPensamientos.map((pensamiento) => (
          <article class="zen-card" style="background: var(--bg-card-warm);">
            <span class="inv-code" style="margin-bottom: 8px;">
              {pensamiento.data.date.toLocaleDateString('es-ES', { year: 'numeric', month: 'short', day: 'numeric' })}
            </span>
            <h3 style="font-family: var(--font-display); font-size: 1.25rem; font-weight: 400; margin-bottom: 8px; line-height: 1.3;">
              <a href={`/posts/pensamientos-${pensamiento.id.replace(/\.md$/, '')}/`} style="color: inherit; text-decoration: none;">
                {pensamiento.data.title || 'Fragmento contemplativo'}
              </a>
            </h3>
            {pensamiento.data.mood && (
              <div style="font-size: 0.78rem; color: var(--ink-muted); margin-top: 8px;">Estado: {pensamiento.data.mood}</div>
            )}
          </article>
        ))}
      </div>
    </section>
  )}

</Layout>
